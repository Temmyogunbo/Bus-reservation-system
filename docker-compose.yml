version: '3'
services:
  postgres:
    image: postgres:12
    volumes:
      - ./postgres-data:/var/lib/postgresql/data
    ports:
      - '5432:5432'
    environment:
      - POSTGRES_PASSWORD=postgres
      - TZ=Etc/UTC
  redis:
     image: redis
     ports:
       - '6379:6379'
